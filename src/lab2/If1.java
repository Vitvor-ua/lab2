package lab2;

/**
 * The If1 interface extends the If2 interface and declares an additional method
 * meth1() to be implemented by any class that implements If1.
 */
public interface If1 extends If2 {
    /**
     * Declares the meth1() method which must be implemented by any class that implements If1.
     */
    void meth1();
}
