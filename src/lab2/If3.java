package lab2;

/**
 * The If3 interface extends the If1 interface and declares an additional method
 * meth3() to be implemented by any class that implements If3.
 */
public interface If3 extends If1 {
    /**
     * Declares the meth3() method which must be implemented by any class that implements If3.
     */
    void meth3();
}
